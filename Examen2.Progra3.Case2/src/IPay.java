
public interface IPay {
	
	/*
	 * La interface IPay determina un metodo que vamos a implementar en la clase Payment
	 * el cual nos devolverá un cálculo a futuro. 
	 * 
	 * */
	
    double payment(int devices, double amount);
}